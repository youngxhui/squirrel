import { ThemedText } from "@/components/ThemedText";
import { SafeAreaView } from "react-native";
export default function Settings() {
  return (
    <SafeAreaView>
      <ThemedText>Settings 🐿️</ThemedText>
    </SafeAreaView>
  );
}
